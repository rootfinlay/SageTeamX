REM --user Is used so admin rights are not required to install.
REM It also means it will only atempt to install the required libraries for you.
pip install Pillow --user
pip install Django==2.2.3 --user
pip install pylint-django --user